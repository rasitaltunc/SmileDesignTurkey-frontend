import React from "react";

export default function DoctorSettings() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-bold">Doctor Settings</h1>
      <p className="text-sm text-gray-600 mt-2">
        If this page was previously white, ErrorBoundary will now show the crash reason.
      </p>
    </div>
  );
}
